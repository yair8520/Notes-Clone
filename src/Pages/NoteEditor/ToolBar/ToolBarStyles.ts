import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {},
  toolbar: {
    marginBottom: 5,
  },
  selectedButton: { backgroundColor: '#B0B0B0', borderRadius: 100 },
});
