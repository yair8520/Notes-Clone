import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  labelContainer: {
    alignItems: 'center',
    justifyContent: 'center',
    height: 60,
    backgroundColor: 'white',
  },
  focusedTab: { color: '#1d9df3' },
});
