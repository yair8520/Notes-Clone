import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    width: '100%',
    borderColor: '#f6f6f6',
  },
  input: {
    width: '100%',
  },
  outline: { borderRadius: 15 },
});
