import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {},
  divider: { height: 2 },
  add: {
    paddingLeft: 5,

    flexDirection: 'row',
    alignItems: 'center',
  },
});
